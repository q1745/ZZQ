<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.usercenter">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name="com.example.common.app.MyApplication"
        android:allowBackup="true"
        android:icon="@drawable/tianxuan"
        android:label="@string/app_name"
        android:roundIcon="@drawable/tianxuan"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mvp">
        <activity android:name=".ui.LoginActivity"/>
        <activity android:name=".ui.YzCodeActivity"/>
        <activity android:name=".ui.MainActivity"/>
        <activity android:name=".ui.RegActivity"/>
        <activity android:name=".ui.SearchActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>